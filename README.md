# CS81 Module 3A - GitHub Practice

This is my class repository for CS81 Module 3a.

## Functions in journal.js

- `greet(name)` — Returns a friendly hello message
- `isEven(number)` — True if the number is even
- `square(num)` — Returns the square of a number
- `describeWeather(temp)` — Describes how the temperature feels
- `addWithMessage(a, b)` — Adds two numbers and returns a nice message

Created by Chip
